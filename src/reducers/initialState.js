import * as key from '../store/constants';

// initial state
const initialState = {
  [key.ERRORS]: {},
  [key.THEME]: true,
  [key.LOADING]: {},
  [key.LOCATIONS]: []
};

export default initialState;
